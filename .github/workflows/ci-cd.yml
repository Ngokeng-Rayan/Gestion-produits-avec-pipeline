name: Gestion Produit Pipeline

# Déclenchement du pipeline
on:
  push:
    branches: [ "**" ]
  pull_request:
    branches: [ "**" ]

# Variables d'environnement globales
env:
  PHP_VERSION: '8.2'
  NODE_VERSION: '20'

jobs:
  # ==========================================
  # JOB 1 : PREPARATION - Installation des dépendances
  # ==========================================
  preparation:
    name: Préparation & Installation des Dépendances
    runs-on: ubuntu-latest

    steps:
      - name: Récupération du code source
        uses: actions/checkout@v4

      - name: Configuration de PHP ${{ env.PHP_VERSION }}
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ env.PHP_VERSION }}
          extensions: mbstring, xml, ctype, json, bcmath, pdo, pdo_mysql, zip, gd
          coverage: none
          tools: composer:v2

      - name: Mise en cache des dépendances Composer
        uses: actions/cache@v4
        with:
          path: vendor
          key: ${{ runner.os }}-composer-${{ hashFiles('**/composer.lock') }}
          restore-keys: |
            ${{ runner.os }}-composer-

      - name: Installation des dépendances Composer
        run: composer install --prefer-dist --no-progress --no-interaction --optimize-autoloader

      - name: Sauvegarde du dossier vendor
        uses: actions/upload-artifact@v4
        with:
          name: vendor-${{ github.sha }}
          path: vendor/
          retention-days: 1

  # ==========================================
  # JOB 2 : LINT - Vérification du style de code
  # ==========================================
  verification-style:
    name: Vérification du Style de Code
    runs-on: ubuntu-latest
    needs: preparation

    steps:
      - name: Récupération du code source
        uses: actions/checkout@v4

      - name: Configuration de PHP ${{ env.PHP_VERSION }}
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ env.PHP_VERSION }}
          extensions: mbstring, xml, ctype, json
          coverage: none

      - name: Téléchargement du dossier vendor
        uses: actions/download-artifact@v4
        with:
          name: vendor-${{ github.sha }}
          path: vendor/

      - name: Exécution de Laravel Pint
        run: php vendor/bin/pint --test

  # ==========================================
  # JOB 3 : SECURITY SCAN - Analyse statique
  # ==========================================
  analyse-securite:
    name: Analyse de Sécurité & Statique
    runs-on: ubuntu-latest
    needs: preparation

    steps:
      - name: Récupération du code source
        uses: actions/checkout@v4

      - name: Configuration de PHP ${{ env.PHP_VERSION }}
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ env.PHP_VERSION }}
          extensions: mbstring, xml, ctype, json
          coverage: none

      - name: Téléchargement du dossier vendor
        uses: actions/download-artifact@v4
        with:
          name: vendor-${{ github.sha }}
          path: vendor/

      - name: Exécution de l'analyse PHPStan
        run: php vendor/bin/phpstan analyse --memory-limit=2G
        continue-on-error: true

      - name: Vérification des vulnérabilités des dépendances
        run: composer audit --no-interaction
        continue-on-error: true

  # ==========================================
  # JOB 4 : TESTS - Tests unitaires et d'intégration
  # ==========================================
  tests:
    name: Tests Unitaires & d'Intégration
    runs-on: ubuntu-latest
    needs: preparation

    # Service MySQL pour les tests d'intégration
    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_ROOT_PASSWORD: root_password
          MYSQL_DATABASE: testing_db
          MYSQL_USER: testing_user
          MYSQL_PASSWORD: testing_password
        ports:
          - 3306:3306
        options: >-
          --health-cmd="mysqladmin ping"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=3

    steps:
      - name: Récupération du code source
        uses: actions/checkout@v4

      - name: Configuration de PHP ${{ env.PHP_VERSION }}
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ env.PHP_VERSION }}
          extensions: mbstring, xml, ctype, json, bcmath, pdo, pdo_mysql, zip, gd
          coverage: none

      - name: Téléchargement du dossier vendor
        uses: actions/download-artifact@v4
        with:
          name: vendor-${{ github.sha }}
          path: vendor/

      - name: Copie du fichier .env
        run: cp .env.example .env

      - name: Génération de la clé d'application
        run: php artisan key:generate

      # Tests unitaires avec SQLite (rapides)
      - name: Création de la base SQLite pour les tests unitaires
        run: touch database/database.sqlite

      - name: Exécution des migrations (SQLite)
        run: php artisan migrate --force
        env:
          DB_CONNECTION: sqlite
          DB_DATABASE: database/database.sqlite

      - name: Exécution des Tests Unitaires
        run: php artisan test --testsuite=Unit
        env:
          DB_CONNECTION: sqlite
          DB_DATABASE: database/database.sqlite

      # Tests d'intégration avec MySQL (plus réalistes)
      - name: Exécution des migrations (MySQL)
        run: php artisan migrate --force
        env:
          DB_CONNECTION: mysql
          DB_HOST: 127.0.0.1
          DB_PORT: 3306
          DB_DATABASE: testing_db
          DB_USERNAME: testing_user
          DB_PASSWORD: testing_password

      - name: Peuplement de la base de données pour les tests
        run: php artisan db:seed --force
        env:
          DB_CONNECTION: mysql
          DB_HOST: 127.0.0.1
          DB_PORT: 3306
          DB_DATABASE: testing_db
          DB_USERNAME: testing_user
          DB_PASSWORD: testing_password

      - name: Exécution des Tests d'Intégration
        run: php artisan test --testsuite=Feature
        env:
          DB_CONNECTION: mysql
          DB_HOST: 127.0.0.1
          DB_PORT: 3306
          DB_DATABASE: testing_db
          DB_USERNAME: testing_user
          DB_PASSWORD: testing_password

      - name: Sauvegarde des résultats des tests
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: resultats-tests-${{ github.sha }}
          path: |
            storage/logs/
            tests/results/
          retention-days: 7

  # ==========================================
  # JOB 5 : DOCKER BUILD - Construction de l'image
  # ==========================================
  construction-docker:
    name: Construction de l'Image Docker
    runs-on: ubuntu-latest
    needs: [verification-style, analyse-securite, tests]

    steps:
      - name: Récupération du code source
        uses: actions/checkout@v4

      - name: Configuration de Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Connexion à Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Extraction des métadonnées (tags, labels)
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ secrets.DOCKER_USERNAME }}/gestion-produit-api
          tags: |
            type=ref,event=branch
            type=sha,prefix=sha-

      - name: Construction et publication de l'image Docker
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          build-args: |
            user=laravel
            uid=1000
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Affichage des informations de l'image
        run: |
          echo "Image Docker construite et publiée avec succès"
          echo "Tags créés:"
          echo "${{ steps.meta.outputs.tags }}"

  # ==========================================
  # JOB 6 : DEPLOY PRODUCTION - Déploiement sur Docker Hub
  # ==========================================
  deploiement-production:
    name: Déploiement en Production
    runs-on: ubuntu-latest
    needs: construction-docker
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'

    steps:
      - name: Connexion à Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Récupération de la date pour le versioning
        id: date
        run: echo "date=$(date +'%Y.%m.%d')" >> $GITHUB_OUTPUT

      - name: Re-tagging de l'image pour la production
        run: |
          # Téléchargement de l'image construite avec le tag SHA
          docker pull ${{ secrets.DOCKER_USERNAME }}/gestion-produit-api:sha-${{ github.sha }}
          
          # Tag latest (dernière version)
          docker tag ${{ secrets.DOCKER_USERNAME }}/gestion-produit-api:sha-${{ github.sha }} \
                     ${{ secrets.DOCKER_USERNAME }}/gestion-produit-api:latest
          
          # Tag avec version datée (pour rollback)
          docker tag ${{ secrets.DOCKER_USERNAME }}/gestion-produit-api:sha-${{ github.sha }} \
                     ${{ secrets.DOCKER_USERNAME }}/gestion-produit-api:v${{ steps.date.outputs.date }}
          
          # Publication des tags
          docker push ${{ secrets.DOCKER_USERNAME }}/gestion-produit-api:latest
          docker push ${{ secrets.DOCKER_USERNAME }}/gestion-produit-api:v${{ steps.date.outputs.date }}

      - name: Résumé du déploiement
        run: |
          echo "Déploiement réussi"
          echo "Image: ${{ secrets.DOCKER_USERNAME }}/gestion-produit-api"
          echo "Tags: latest, v${{ steps.date.outputs.date }}"
          echo "Docker Hub: https://hub.docker.com/r/${{ secrets.DOCKER_USERNAME }}/gestion-produit-api"
          echo "SHA: ${{ github.sha }}"
          echo "Auteur: ${{ github.actor }}"
          echo "Commit: ${{ github.event.head_commit.message }}"

      - name: Nettoyage des anciennes images (optionnel)
        run: |
          echo "Pensez à nettoyer les anciennes images sur Docker Hub pour économiser de l'espace"
          echo "Conserver: latest, v${{ steps.date.outputs.date }}, et les 5 dernières versions"
